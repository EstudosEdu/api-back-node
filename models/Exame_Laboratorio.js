const { DataTypes } = require('sequelize');

const modelExameLaboratorio = {
    name: 'ExameLaboratorio',
    table: {
    },
    options: {freezeTableName: true}
}

module.exports = modelExameLaboratorio;